<section style="margin-top: 150px;">
    <div class="row">
        <div class="containe p-5 d-flex">
            <div class="col-md-12 text-center ">
                <h1 class="text-center mt-5">Address List</h1>

                {{!-- <table class="table table-hover mb-0">
                    <thead>
                        <tr class="align-self-center">
                            <th>Name</th>
                            <th>Mobile number</th>
                            <th>Pincode</th>
                            <th>Address</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each address}}
                        <tr>
                            <td>{{this.addressDetails.Name}}</td>
                            <td>{{this.addressDetails.mobile}}</td>
                            <td>{{this.addressDetails.pincode}}</td>
                            <td>{{this.addressDetails.address}}</td>
                            <td>
                                <a href="delet-address/{{this._id}}" class="btn btn-sm btn-danger"
                                    onclick="return confirm('Are you want to delete {{this._id}} ?')">
                                    Delete
                            </td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table> --}}



                {{#each address}}
                <div class="col-md-6 mx-auto mt-5  d-flex" id="address-list">
                    <div class="col-md-4 p-5">
                        <h6 class="product-title"> {{this.Name}}</h6>
                        <h3 class="pro-price">{{this.mobile}}</h3>

                    </div>
                    <div class="col-md-4 p-5">
                        <p class="product-title">{{this.address}}</p>
                        <p class="product-title">{{this.district}}</p>
                        <h4 class="pro-price">{{this.pincode}}</h4>
                    </div>
                    <div class="col-md-4 p-5">
                        <button type="button" ></button>
                        <a href="address-edit/{{this._id}}" class="btn btn-sm btn-success">
                            <i class="fas fa-edit"></i></a>
                        <a href="delet-address/{{this._id}}" class="btn btn-sm btn-danger"
                            onclick="return confirm('Are you want to delete {{this._id}} ?')">
                            <i class="fas fa-trash-alt"></i></a>
                    </div>


                </div>
                {{/each}}
                <!--end table-responsive-->


            </div>
        </div>
    </div>
</section>